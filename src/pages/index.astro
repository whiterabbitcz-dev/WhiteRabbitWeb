---
import Layout from '../layouts/Layout.astro';
import HeroSlideshow from '../components/HeroSlideshow.jsx';
import homeData from '../data/home.json';

// Data pro hero slideshow
const heroImages = [
  '/ukazky_aquila_3.png',
  '/ukazky_doritos_2.png',
  '/ukazky_hanacka_1.png',
  '/ukazky_nutribullet_1.png',
  '/ukazky_aquila_2.png',
];
---

<Layout title="White Rabbit - Kreativita bez hranic">
  
  <!-- Navigation - Centrované menu -->
  <nav class="fixed top-6 left-1/2 -translate-x-1/2 z-50 w-full max-w-screen-xl px-6">
    <div class="flex items-center justify-center gap-4 md:gap-6">
      <!-- Logo -->
      <a href="#home" class="flex items-center flex-shrink-0">
        <img src="/logo-whiterabbit-light.svg" alt="White Rabbit" class="h-8 w-auto drop-shadow-lg" />
      </a>
      
      <!-- Odkazy jako tlačítka -->
      <a href="#services" class="px-3 md:px-5 py-2 bg-white/5 backdrop-blur-md border border-white/10 rounded-full text-sm font-medium text-white whitespace-nowrap transition-all hover:bg-white hover:text-black hover:border-white">
        Služby
      </a>
      <a href="#portfolio" class="px-3 md:px-5 py-2 bg-white/5 backdrop-blur-md border border-white/10 rounded-full text-sm font-medium text-white whitespace-nowrap transition-all hover:bg-white hover:text-black hover:border-white">
        Práce
      </a>
      <a href="#about" class="px-3 md:px-5 py-2 bg-white/5 backdrop-blur-md border border-white/10 rounded-full text-sm font-medium text-white whitespace-nowrap transition-all hover:bg-white hover:text-black hover:border-white">
        O nás
      </a>
    </div>
  </nav>

  <!-- Hero Section - Dvousloupcový -->
  <section id="home" class="min-h-[80vh] flex items-center px-6 pt-32 pb-12 md:pt-40 md:pb-16 lg:pt-48">
    <div class="max-w-7xl mx-auto w-full">
      <div class="grid lg:grid-cols-2 gap-8 lg:gap-12 items-center">
        
        <!-- Levý sloupec - Text -->
        <div class="order-2 lg:order-1">
          <h1 class="cms-editable font-serif text-4xl md:text-5xl lg:text-6xl mb-6 leading-tight text-white">
            {homeData.heroTitle}
          </h1>
          
          <p class="cms-editable text-base md:text-lg text-gray-400 mb-8 max-w-md leading-relaxed">
            {homeData.heroSubtitle}
          </p>
          
          <div class="flex flex-row gap-3 md:gap-4">
            <a href="#portfolio" class="px-4 md:px-6 py-3 bg-white text-black rounded-full text-sm md:text-base font-medium hover:bg-gray-100 transition whitespace-nowrap">
              {homeData.buttonWork}
            </a>
            <a href="#contact" class="px-4 md:px-6 py-3 border border-white/20 rounded-full text-sm md:text-base font-medium hover:bg-white/5 transition whitespace-nowrap">
              {homeData.buttonContact}
            </a>
          </div>
        </div>
        
        <!-- Pravý sloupec - Slideshow -->
        <div class="order-1 lg:order-2 flex justify-center lg:justify-end">
          <div class="max-w-sm md:max-w-md w-full h-[500px] md:h-[600px] shadow-2xl">
            <HeroSlideshow images={heroImages} client:load />
          </div>
        </div>
        
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section id="services" class="py-12 md:py-16 px-6">
    <div class="max-w-7xl mx-auto">
      <h2 class="font-serif text-3xl md:text-4xl lg:text-5xl mb-3 text-center">Co děláme</h2>
      <p class="text-center text-gray-400 mb-10 max-w-2xl mx-auto text-sm md:text-base">
        Strategii & konzultace • Obsah & produkci • Správu & reporting • Influencery & UGC • Performance & kampaně
      </p>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-4">
        
        {homeData.services.map((service) => (
          <div class="group p-6 rounded-2xl border border-white/10 hover:border-white/20 transition bg-white/5">
            <h3 class="cms-editable font-serif text-xl mb-3">{service.title}</h3>
            <p class="cms-editable text-gray-400 text-sm leading-relaxed">
              {service.description}
            </p>
          </div>
        ))}

      </div>
    </div>
  </section>

  <!-- Portfolio Section -->
  <section id="portfolio" class="py-12 md:py-16 px-6">
    <div class="max-w-7xl mx-auto">
      <h2 class="font-serif text-3xl md:text-4xl lg:text-5xl mb-3 text-center">Ukázky práce</h2>
      <p class="text-center text-gray-400 mb-10 text-sm md:text-base">
        Díla, na která jsme hrdí
      </p>
      
      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
        
        {homeData.portfolio.map((project) => (
          <div class={`group relative overflow-hidden rounded-xl aspect-[4/5] border border-white/10 ${project.size === 'wide' ? 'md:col-span-2' : 'md:col-span-1'}`}>
            <img src={project.image} alt={project.client} class="w-full h-full object-cover group-hover:scale-105 transition duration-500" />
            <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 group-hover:opacity-100 transition flex items-end p-4">
              <div>
                <h3 class="font-serif text-base md:text-lg">{project.client}</h3>
                <p class="text-xs text-gray-300">{project.category}</p>
              </div>
            </div>
          </div>
        ))}
        
      </div>
    </div>
  </section>

  <!-- Reels Section -->
  <section class="py-12 md:py-16 px-6">
    <div class="max-w-7xl mx-auto">
      <h2 class="font-serif text-3xl md:text-4xl lg:text-5xl mb-3 text-center">Reels na Instagramu</h2>
      <p class="text-center text-gray-400 mb-10 text-sm md:text-base">
        Krátká videa, která fungují
      </p>
      
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4 max-w-5xl mx-auto">
        
        {homeData.reels.map((reel) => (
          <a 
            href={reel.url} 
            target="_blank" 
            rel="noopener noreferrer"
            class="group relative overflow-hidden rounded-xl aspect-[9/16] border border-white/10 hover:border-white/30 transition-all"
          >
            <img src={reel.image} alt={reel.title} class="w-full h-full object-cover group-hover:scale-105 transition duration-500" />
            
            <!-- Play button overlay - vždy viditelné -->
            <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
              <div class="w-16 h-16 md:w-20 md:h-20 rounded-full bg-white/20 backdrop-blur-sm flex items-center justify-center transition-all group-hover:scale-110 group-hover:bg-white/40">
                <svg class="w-8 h-8 md:w-10 md:h-10 text-white ml-1" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M8 5v14l11-7z"/>
                </svg>
              </div>
            </div>
          </a>
        ))}
        
      </div>
    </div>
  </section>

  <!-- About Section -->
  <section id="about" class="py-12 md:py-16 px-6">
    <div class="max-w-4xl mx-auto text-center">
      <h2 class="font-serif text-3xl md:text-4xl lg:text-5xl mb-6">Jak pracujeme</h2>
      <p class="text-gray-400 leading-relaxed mb-8 text-sm md:text-base">
        Bez omáček, bez zbytečných slov, bez složitých procesů.
      </p>
      
      <ul class="text-left text-gray-400 space-y-3 max-w-2xl mx-auto text-sm md:text-base">
        <li class="flex items-start gap-3">
          <span class="text-white mt-1">•</span>
          <span>Tvoříme efektivně a rychle.</span>
        </li>
        <li class="flex items-start gap-3">
          <span class="text-white mt-1">•</span>
          <span>Víme, že důležitý může být všední den, ne jenom na kontroly.</span>
        </li>
        <li class="flex items-start gap-3">
          <span class="text-white mt-1">•</span>
          <span>Na každý projekt se díváme jako na vlastní.</span>
        </li>
      </ul>
    </div>
  </section>

  <!-- O nás -->
  <section class="py-12 md:py-16 px-6">
    <div class="max-w-4xl mx-auto text-center">
      <h2 class="cms-editable font-serif text-3xl md:text-4xl lg:text-5xl mb-6">{homeData.about.title}</h2>
      {homeData.about.text.split('\n\n').map((paragraph) => (
        <p class="cms-editable text-gray-400 leading-relaxed text-lg md:text-xl mt-4 first:mt-0">
          {paragraph}
        </p>
      ))}
    </div>
  </section>

  <!-- Footer - Contact -->
  <footer id="contact" class="py-12 md:py-16 px-6 text-center">
    <div class="max-w-4xl mx-auto">
      <h2 class="cms-editable font-serif text-2xl md:text-3xl mb-4">{homeData.contact.title}</h2>
      <p class="cms-editable text-gray-400 mb-8 text-sm md:text-base">{homeData.contact.subtitle}</p>
      
      <a href={`mailto:${homeData.contact.email}`} class="font-serif text-3xl md:text-4xl lg:text-5xl hover:text-gray-300 transition inline-block mb-8">
        {homeData.contact.email}
      </a>
      
      <div class="flex flex-col md:flex-row gap-4 md:gap-6 justify-center mb-8 mt-6">
        <a href={homeData.contact.instagramUrl} target="_blank" class="text-gray-400 hover:text-white transition text-sm">
          Instagram
        </a>
        <a href={homeData.contact.linkedinUrl} target="_blank" class="text-gray-400 hover:text-white transition text-sm">
          LinkedIn
        </a>
      </div>
      
      <p class="text-gray-600 text-sm">
        {homeData.contact.footerText}
      </p>
    </div>
  </footer>

</Layout>
